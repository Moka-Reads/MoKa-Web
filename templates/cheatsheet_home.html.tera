<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/devicons/devicon@v2.15.1/devicon.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Russo+One&display=swap">
    <title>MoKa Reads | Cheatsheets</title>
    <meta name="description" content="An Opensource Education Platform">
    {% include "style" %}
</head>


<body>

    <header>
        <h1 style="font-family: 'Russo One', sans-serif;">MoKa Reads Cheatsheets</h1>
    </header>

    {% include "nav" %}

    <div class="cheatsheets-container">
        <div class="language-group">
            <h2><i class="devicon-rust-plain"></i> Rust</h2>
            <ul class="cheatsheets-grid">
                {% for r in rust %}
                <li><a href="/cheatsheets/{{r.slug}}">{{r.title}}</a></li>
                {% endfor %}
            </ul>
        </div>
        <div class="language-group">
            <h2><i class="devicon-kotlin-plain"></i> Kotlin</h2>
            <ul class="cheatsheets-grid">
                {% for k in kotlin %}
                <li><a href="/cheatsheets/{{k.slug}}">{{k.title}}</a></li>
                {% endfor %}
            </ul>
        </div>
        <div class="language-group">
            <h2><i class="devicon-c-plain"></i> C</h2>
            <ul class="cheatsheets-grid">
                {% for v in c %}
                <li><a href="/cheatsheets/{{v.slug}}">{{v.title}}</a></li>
                {% endfor %}
            </ul>
        </div>
        <div class="language-group">
            <h2><i class="devicon-cplusplus-plain"></i> C++</h2>
            <ul class="cheatsheets-grid">
                {% for v in cpp %}
                <li><a href="/cheatsheets/{{v.slug}}">{{v.title}}</a></li>
                {% endfor %}
            </ul>
        </div>
        <div class="language-group">
            <h2><i class="devicon-zig-original"></i> Zig</h2>
            <ul class="cheatsheets-grid">
                {% for z in zig %}
                <li><a href="/cheatsheets/{{z.slug}}">{{z.title}}</a></li>
                {% endfor %}
            </ul>
        </div>
        <div class="language-group">
            <h2><i class="devicon-python-plain"></i> Python</h2>
            <ul class="cheatsheets-grid">
                {% for p in python %}
                <li><a href="/cheatsheets/{{p.slug}}">{{p.title}}</a></li>
                {% endfor %}
            </ul>
        </div>
        <div class="language-group">
            <h2><i class="devicon-swift-plain"></i> Swift</h2>
            <ul class="cheatsheets-grid">
                {% for s in swift %}
                <li><a href="/cheatsheets/{{s.slug}}">{{s.title}}</a></li>
                {% endfor %}
            </ul>
        </div>
        <div class="language-group">
            <h2><i class="devicon-go-plain"></i> Go</h2>
            <ul class="cheatsheets-grid">
                {% for g in go %}
                <li><a href="/cheatsheets/{{g.slug}}">{{g.title}}</a></li>
                {% endfor %}
            </ul>
        </div>
        <div class="language-group">
            <h2><i class="devicon-github-original"></i> Other</h2>
            <ul class="cheatsheets-grid">
                {% for o in other %}
                <li><a href="/cheatsheets/{{o.slug}}">{{o.title}}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>


    {%
    include "footer"
    %}

</body>

</html>